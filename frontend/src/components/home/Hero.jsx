import Button from "../common/Button";

function Hero() {
	return (
		<div className="hero">
			<h1 className="hero-title">Hi!</h1>
			<p className="hero-content">I am Coleman Matey</p>
			<Button text="Explore" style="explore-btn" />
		</div>
	);
}

export default Hero;
